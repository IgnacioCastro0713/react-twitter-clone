import React, {Fragment} from "react";
import SuggestionFollow from "./shared/SuggestionFollow";
import Options from "./shared/Options";

export default () => {
	return (
		<Fragment>
			<SuggestionFollow/>
			<Options/>
		</Fragment>
	);
}